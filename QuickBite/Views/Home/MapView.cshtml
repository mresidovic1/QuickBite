<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mapa Sarajeva</title>
    <style>
        body, html {
            margin: 0;
            padding: 0;
            width: 100%;
            height: 100%;
            background: url('/images/sarajevomapa.jpg') no-repeat center center fixed;
            background-size: cover;
            font-family: Arial, sans-serif;
        }

        .header {
            background-color: #009688;
            padding: 10px 20px;
            display: flex;
            align-items: center;
            color: white;
            font-size: 24px;
            font-weight: bold;
        }

        .container {
            position: fixed;
            bottom: 20px;
            right: 20px;
            background: rgba(0, 150, 136, 0.9);
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            width: 300px;
        }

        .location-form {
            display: flex;
            flex-direction: column;
        }

            .location-form label {
                margin-bottom: 10px;
                font-weight: bold;
                color: white;
            }

            .location-form input {
                margin-bottom: 10px;
                padding: 10px;
                border: 1px solid #ccc;
                border-radius: 4px;
                font-size: 16px;
            }

            .location-form button {
                padding: 10px;
                border: none;
                border-radius: 4px;
                background-color: #4CAF50;
                color: white;
                font-size: 16px;
                cursor: pointer;
            }

                .location-form button:hover {
                    background-color: #45a049;
                }
    </style>
</head>
<body>
    <div class="header">QuickBite</div>
    <div class="container">
        <form class="location-form" method="post" action="@Url.Action("PotvrdiLokaciju", "Home")">
            <label for="location">Vaša adresa:</label>
            <input type="text" id="location" name="location" placeholder="Unesi adresu">
            <button type="submit">Potvrdi lokaciju</button>
        </form>

    </div>
</body>
</html>
